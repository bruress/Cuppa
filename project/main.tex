\documentclass[utf8, russian]{eskdtext}
\usepackage{paralist}
\newcommand{\No}{\textnumero}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage{microtype}
\usepackage{rotating, graphicx}
\graphicspath{
    {../Menu/Assets/Background/}
    {../Novel/Assets/Novel/}
    {../Novel/Assets/Sprites/Kori/}
    {../Novel/Assets/Sprites/Mary/}
    {../Novel/Assets/Sprites/Rose/}
    {../RhythmGame/Assets/Sprites/Notes/}
    {../RhythmGame/Assets/Sprites/Judgment/}
    {../RhythmGame/Assets/Sprites/Ligths/}
    {../RhythmGame/Assets/VFX/Effects/}
}
\usepackage{mwe} 
\usepackage{wrapfig}
\usepackage{caption}
\usepackage{float}
\usepackage[hidelinks]{hyperref}

\setlength{\parindent}{1.0cm}

% Русская нумерация без пакетов
\makeatletter
\def\rusenum#1{%
    \expandafter\ifcase\csname c@#1\endcsname
    \or а%
    \or б%
    \or в%
    \or г%
    \or д%
    \or е%
    \or е%
    \or ж%
    \or з%
    \or и%
    \or й%
    \or к%
    \or л%
    \or м%
    \or н%
    \or о%
    \or п%
    \or р%
    \or с%
    \or т%
    \or у%
    \or ф%
    \or х%
    \or ц%
    \or ч%
    \or ш%
    \or щ%
    \or ъ%
    \or ы%
    \or ь%
    \or э%
    \or ю%
    \or я%
    \fi
}
\renewcommand{\labelenumi}{\rusenum{enumi})}
\renewcommand{\theenumi}{\rusenum{enumi}}
\makeatother

% Счетчики
\newcounter{game_cnt}
\setcounter{game_cnt}{1}

\newcounter{img_cnt}
\setcounter{img_cnt}{1}

\usepackage{titlesec} 
\titleformat{\section}
{\normalsize\bfseries} 
{\thesection} {1em}{} 
\titleformat{\subsection}
{\normalsize\bfseries}
{\thesubsection} {1em}{} 
\titleformat{\subsubsection}
{\normalsize\bfseries}
{\thesubsubsection} {1em}{}

% Для штампа
\ESKDtitle{{\smallРазработка однопользовательской 2D игры <<Cuppa>>}}
\ESKDsignature{ТПЖА.000000.000 ПЗ}
\ESKDgroup{\footnotesize Кафедра ЭВМ\\ИВТб-2302-05-00}
\ESKDauthor{Матешко}
\ESKDchecker{Сергеева}

% Каждый раздел (\section) с новой страницы
\let\stdsection\section
\renewcommand \section{\newpage\stdsection}

\title{Разработка однопользовательской 2D игры <<Cuppa>>}

\usepackage{enumitem}
\renewcommand{\labelitemi}{\normalfont\bfseries{--}}
\setlist{nosep,
left=0pt,
labelindent=\parindent,leftmargin=*,
leftmargin=0pt,itemindent=\labelwidth
}
\setlist[enumerate,2]{
leftmargin=\parindent,itemindent=\labelwidth
}
\setenumerate{listparindent=\parindent}
\makeatletter
\AddEnumerateCounter{\asbuk}{\@asbuk}{м)}
\makeatother
\renewcommand{\labelenumi}{\asbuk{enumi})} 
\renewcommand{\labelenumii}{\arabic{enumii})}
  \renewcommand{\thesection}{\arabic{section}}
  \renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}


% Выравнивание и переносы %%%
\sloppy                             % Избавляемся от переполнений
\clubpenalty=10000                  % Запрещаем разрыв страницы после первой строки абзаца
\widowpenalty=10000                 % Запрещаем разрыв страницы после последней строки абзаца

% Для кода
\usepackage{listings}
\usepackage{verbatim}

% Для ссылок


\begin{document}
\maketitle
% Содержание
\tableofcontents

\sloppy
\newpage

\section*{Введение}

В современном обществе развлечения являются важной частью досуга. Одним из направлений этой сферы является индустрия видеоигр. Развитие цифровых технологий расширило возможности создания игр, тем самым, сделав формат игр вариативнее.

В современной индустрии развлечений видеоигры занимают отдельное место. Пользователям интересны как AAA-проекты, так и инди-игры, разрабатываемые как одиночными разработчиками, так и командами энтузиастов.

Разработчики в этой отрасли стремятся предложить игрокам новые форматы взаимодействия, оригинальные истории, дизайн и уникальные механики, которые впоследствии могут привести к появлению нового жанра.

В жанре ритм-игр основой игрового процесса является попадание по <<нотам>> -- движущимся объектам -- в ритм музыки. Такой формат задает активное взаимодействие пользователя с игрой.

Таким образом, у меня возникла идея создать собственную игру в жанре ритм-игры с акцентом на атмосферу и повествование.

Однако я преследую цель создать игру, в которой внешний комфорт сочетается с мрачным вампирским миром и напряженным повествованием. Чтобы реализовать такой контраст, я пришла к идее разработать визуальную новеллу про вампирскую кофейню, где главный герой -- бариста. В истории поднимаются темы внутреннего конфликта персонажей и последствий результатов прохождения, включая неблагоприятные концовки. В игровой процесс встроен элемент ритм-игры, отвечающий за приготовление кофе.

\textbf{Цель проекта: } разработать оригинальную однопользовательскую 2D-игру, в которой присутствует взаимодействие с ритмом музыки и простой сюжет. В ходе игры пользователь будет общаться с вампирами и готовить кофе клиентам, играя в ритм-игру, тем самым зарабатывая очки, от которых зависит развитие истории и концовка игры -- будет ли существовать вампирская кофейня или нет.

\textbf{Задачи проекта: }

\begin{enumerate}
    \item Проанализировать существующие проекты и выделить механики, применимые в рамках выбранных жанров;
    \item Сформировать требования к проекту;
    \item Спроектировать архитектуру игры, определив алгоритмы поведения и структуру состояний;
    \item Разработать базовые модули и реализовать их с использованием игрового движка Godot;
    \item Провести тестирование, проанализировать результаты и определить потенциал дальнейшего развития.
\end{enumerate}

\textbf{Объект исследования: } однопользовательские 2D-игры, визуальные новеллы, ритм-игры, игровые системы интерактивного повествования и переходы между игровыми состояниями.

\textbf{Предмет исследования: } однопользовательская 2D-игра <<Cuppa>>, сочетающая визуальную новеллу и ритм-механику приготовления кофе, а также алгоритмы обработки нот, оценки действий игрока, отображения сюжетных сцен, переходов между игровыми состояниями и формирования сюжетных концовок.

\textbf{Актуальность проекта} в рамках данной работы обусловлена сочетанием следующих факторов:

\begin{enumerate}
  \item Выбор видеоигр как одного из форматов досуга;
  \item Значимая роль музыкального сопровождения в формировании атмосферы игрового процесса;
  \item Применение сюжетного подхода, в котором спокойная внешняя обстановка сочетается с более напряженным повествованием;
  \item Тематическая выразительность кофейной эстетики в сочетании с вампирским сеттингом;
  \item Интерес к совмещению жанров как к способу формирования более цельного игрового опыта.
\end{enumerate}

\newpage

\section{Анализ предметной области}

\textbf{Ритм-игры} -- это жанр видеоигр, в основе которого лежит своевременное выполнение действий в соответствии с ритмом музыкального сопровождения. Точность действий влияет на набор очков, качество прохождения и итоговый результат.

\textbf{Визуальная новелла} -- это жанр видеоигр, игровой процесс которого строится на чтении текста, выборе реплик или действий, влияющих на развитие истории и ее концовку.

\subsection{Обзор предметной области}

Игровой процесс ритм-игр требует от пользователя концентрации, реакции и чувства ритма, но при этом сохраняет понятную базовую механику взаимодействия.

Вместе с тем во многих проектах данного жанра основной акцент делается на механике, тогда как сюжетная часть и атмосфера получают меньше внимания, либо вовсе отсутствуют. В результате игровой опыт нередко ограничивается только серией действий под музыку.

В рамках проекта <<Cuppa>> эта проблема решается через объединение ритм-игры и визуальной новеллы. Ритм-механика сохраняет динамику и вовлеченность, а повествовательная часть добавляет персонажей, диалоги и развитие истории.

В рамках проекта музыкальные сцены встроены в контекст событий и связаны с развитием повествования. Это реализуется через концепцию вампирской кофейни, где игрок выступает в роли бариста, общается с посетителями и готовит напитки с использованием ритм-элементов.

Таким образом, проект направлен на создание целостного игрового опыта, сочетающего два жанра: ритм-игры и визуальной новеллы.

\subsection{Анализ аналогов}

Для проектирования игровых механик и определения пользовательского интерфейса важно обратиться к анализу существующих аналогов. Такой анализ позволяет понять, какие подходы уже используются в играх данных жанров и какие из них можно адаптировать в рамках собственного проекта.

В рамках данного исследования рассматриваются игры, обладающие преимущественно следующими признаками:

\begin{enumerate}
    \item Наличие главного героя, выступающего в роли обслуживающего персонала (например, бариста, официанта или бармена);
    \item Визуальное исполнение в 2D-стиле;
    \item Принадлежность к жанру визуальной новеллы, ритм-игры или их сочетанию;
    \item Акцент на взаимодействие с персонажами, атмосферу и игровые действия, влияющие на восприятие истории.
\end{enumerate}

\subsubsection{Анализ игры <<VA-11 Hall-A>>}

Игра <<VA-11 Hall-A>> представляет собой визуальную новеллу, в которой игрок выступает в роли бармена в киберпанк-баре. Основной игровой процесс строится вокруг общения с посетителями и приготовления напитков, от которых зависит развитие диалогов и состояние персонажей.

Данный проект полезен для анализа, потому что показывает, как механика обслуживания посетителей может быть связана с повествованием и основным геймплеем. В игре приготовление напитков не является отдельным элементом, а помогает раскрывать характеры персонажей и поддерживать атмосферу места, где происходит действие.

\textbf{Преимущества:}

\begin{itemize}
    \item Интересное сочетание игрового процесса и повествования;
    \item Атмосфера и проработанные персонажи;
    \item Влияние действий игрока на развитие диалогов.
\end{itemize}

\textbf{Недостатки:}

\begin{itemize}
    \item Ограниченность игровых механик.
\end{itemize}

    \begin{figure}[H]
        \centering
        \includegraphics[scale=0.4]{game\arabic{game_cnt}.png}\\
        \caption*{Рисунок \arabic{img_cnt} -- Пример игрового процесса <<VA-11 Hall-A>>}
        \stepcounter{img_cnt}
        \stepcounter{game_cnt}
    \end{figure}

\subsubsection{Анализ игры <<Coffee Talk>>}

Игра <<Coffee Talk>> представляет собой визуальную новеллу, в которой игрок выступает в роли бариста в кофейне. Основной игровой процесс включает приготовление напитков и общение с посетителями, каждый из которых имеет свою историю, настроение и внутренний конфликт.

Данный пример важен для анализа, поскольку в нем кофейня используется не только как место действия, но и как пространство для раскрытия персонажей через диалоги. Для проекта <<Cuppa>> важен именно этот подход: спокойная внешняя обстановка кофейни сочетается с более напряженным или мрачным повествованием.

\textbf{Преимущества:}

\begin{itemize}
    \item Атмосфера и проработанные персонажи;
    \item Понятная механика приготовления напитков;
    \item Акцент на историях посетителей и взаимодействии с ними.
\end{itemize}

\textbf{Недостатки:}

\begin{itemize}
    \item Ограниченность игровых механик;
    \item Небольшое влияние игрока на развитие сюжета.
\end{itemize}

     \begin{figure}[H]
         \centering
         \includegraphics[scale=0.35]{game\arabic{game_cnt}.png}\\
         \caption*{Рисунок \arabic{img_cnt} -- Пример игрового процесса <<Coffee Talk>>}
         \stepcounter{img_cnt}
         \stepcounter{game_cnt}
     \end{figure}

\subsubsection{Анализ игры <<Yunyun Syndrome!? Rhythm Psychosis>>}

Игра <<Yunyun Syndrome!? Rhythm Psychosis>> представляет собой ритм-игру с ярким визуальным стилем и необычной подачей. Игровой процесс строится на своевременном выполнении действий под музыку, а визуальная часть усиливает эмоциональное состояние происходящего.

Данный проект полезен для анализа ритм-механики и связи музыки с визуальным восприятием. В рамках проекта <<Cuppa>> ритм-сцены рассматриваются не как отдельное испытание, а как часть общего настроения и состояния персонажей.

\textbf{Преимущества:}

\begin{itemize}
    \item Выразительный визуальный стиль;
    \item Динамичный и вовлекающий игровой процесс;
    \item Синхронизация визуала и музыки.
\end{itemize}

\textbf{Недостатки:}

\begin{itemize}
    \item Высокая нагрузка на восприятие игрока.
\end{itemize}

     \begin{figure}[H]
         \centering
         \includegraphics[scale=0.35]{game\arabic{game_cnt}.png}\\
         \caption*{Рисунок \arabic{img_cnt} -- Пример игрового процесса <<Yunyun Syndrome!? Rhythm Psychosis Demo>>}
         \stepcounter{img_cnt}
         \stepcounter{game_cnt}
     \end{figure}

\subsubsection{Анализ игры <<Glisynth>>}

Игра <<Glisynth>> представляет собой экспериментальную ритм-игру, в которой основной акцент сделан на взаимодействии игрока с аудиовизуальной средой. Игровой процесс строится на синхронизации действий с музыкой и визуальными эффектами.

В рамках сравнения этот проект показывает, как звук, визуал и действия игрока могут работать как единая система. Для <<Cuppa>> это важно, потому что ритм-механика приготовления кофе должна быть связана не только с набором очков, но и с ощущением процесса, атмосферой кофейни, историей и итоговым результатом сцены.

\textbf{Преимущества:}

\begin{itemize}
    \item Оригинальный подход к ритм-механикам;
    \item Сильная связь между звуком и визуалом;
    \item Атмосферность и экспериментальность.
\end{itemize}

\textbf{Недостатки:}

\begin{itemize}
    \item Отсутствие сюжета;
    \item Ограниченность игровых целей и мотивации.
\end{itemize}

     \begin{figure}[H]
         \centering
         \includegraphics[scale=0.25]{game\arabic{game_cnt}.png}\\
         \caption*{Рисунок \arabic{img_cnt} -- Пример игрового процесса <<Glisynth>>}
         \stepcounter{img_cnt}
         \stepcounter{game_cnt}
     \end{figure}

\subsection{Требования к разработке}

Требования к разрабатываемому программному продукту можно условно разделить на функциональные и нефункциональные. Первые описывают основные возможности системы и игровой процесс, тогда как вторые определяют качество работы, визуальную составляющую и технические ограничения проекта.

\subsubsection{Функциональные требования}

\begin{enumerate}
    \item \textbf{Система диалогов и повествования:} отображение текстовой информации, реплик персонажей и сюжетных сцен, необходимых для развития истории;
    \item \textbf{Ритм-механика приготовления напитков:} реализация игрового процесса, основанного на попадании в ритм музыкального сопровождения;
    \item \textbf{Синхронизация аудио и визуала:} минимальная задержка между звуком и отображением элементов;
    \item \textbf{Обработка игровых событий:} обработка ключевых событий игрового цикла;
    \item \textbf{Система оценки и прогрессии:} определение точности действий игрока с последующим начислением очков, влияющих на качество приготовления напитков и концовку игры;
    \item \textbf{Связь игровых элементов:} реализация взаимодействия с посетителями по схеме «заказ -- приготовление -- отдача», связывающей элементы визуальной новеллы и ритм-игры;
    \item \textbf{Система состояний:} управление переходами между различными режимами игры (меню, диалоги, ритм-игра) без потери текущего прогресса;
    \item \textbf{Пользовательский интерфейс:} отображение диалогов, элементов ритм-игры, количества очков и текущего состояния игрового процесса.
\end{enumerate}

\subsubsection{Нефункциональные требования}

\begin{enumerate}
    \item \textbf{Визуальная стилистика:} 2D в стиле пиксель-арт с вечерней вампирской атмосферой, мягким свечением и контрастом;
    \item \textbf{Музыкальное сопровождение:} музыка с четко выраженным ритмом, подходящая для ритм-сцен;
    \item \textbf{Производительность:} стабильная работа игры на персональном компьютере без заметных просадок;
    \item \textbf{Кроссплатформенность:} корректная и одинаковая работа игровых механик, интерфейса и переходов на Linux и Windows;
    \item \textbf{Надежность:} устойчивость системы к действиям пользователя, способным привести к сбоям;
    \item \textbf{Читаемость текста:} использование элегантных шрифтов, подходящих стилю вампиров, при этом сохраняя удобство восприятия;
    \item \textbf{Удобство взаимодействия:} интуитивно понятная структура интерфейса и однозначная визуальная обратная связь на действия игрока.
\end{enumerate}

\subsection{Постановка задач}

В рамках данного курсового проекта ставится задача по разработке функционального прототипа однопользовательской 2D-игры <<Cuppa>>. Игра будет выполнена в формате визуальной новеллы с элементами ритм-игры, где пользователь выступает в роли бариста вампирской кофейни, взаимодействует с посетителями, проходит сюжетные сцены и готовит напитки через ритм-механику.

Ключевая особенность проекта заключается в сочетании двух жанров и внешне спокойной атмосферы кофейни с напряженным повествованием. Качество прохождения ритм-сцен влияет на набор очков, состояние истории и итоговую концовку. Конечной целью является создание интерактивного продукта, демонстрирующего работу системы отображения диалогов, ритм-механики, системы оценки действий игрока и переходов между основными игровыми состояниями.

Такое программное обеспечение должно включать:

\begin{enumerate}
    \item Главное меню с возможностью запуска игры;
    \item Диалоговые сцены визуальной новеллы с отображением персонажей, текста и сюжетных событий;
    \item Ритм-сцену приготовления напитка с обработкой нот;
    \item Систему оценки точности действий игрока и начисления очков;
    \item Систему переходов между меню, сюжетными сценами, ритм-игрой и экраном окончания;
    \item Несколько вариантов концовок, зависящих от результатов прохождения;
    \item Пользовательский интерфейс, отображающий необходимые игровые данные и обеспечивающий понятное взаимодействие с игрой.
\end{enumerate}

\subsection{Заключение}

В данном разделе пояснительной записки была раскрыта предметная область разработки однопользовательской 2D-игры <<Cuppa>>. Были рассмотрены особенности ритм-игр и визуальных новелл, а также причины выбора их сочетания в рамках одного проекта.

В ходе анализа были изучены существующие игры-аналоги, выделены их преимущества и недостатки, которые могут быть учтены при разработке собственного программного продукта. На основе проведенного обзора были сформулированы функциональные и нефункциональные требования, определяющие основные возможности игры, ее технические ограничения и ожидаемый пользовательский опыт.

Таким образом, по результатам анализа была сформирована постановка задачи на разработку функционального прототипа игры <<Cuppa>>, включающего сюжетные сцены, ритм-механику приготовления напитков, систему оценки действий игрока и переходы между ключевыми состояниями игры.

\section{Проектирование}

В данном разделе будет разработана структура однопользовательской 2D-игры <<Cuppa>>, а также описаны основные состояния, игровые данные и алгоритмы, обеспечивающие работу визуальной новеллы и ритм-механики. Проектирование необходимо для того, чтобы заранее определить связь между меню, сюжетными сценами, ритм-игрой, системой оценки и концовками.

Для данного проекта особенно важно сохранить целостность игрового процесса. Ритм-сцена не должна существовать отдельно от истории, так как именно через приготовление напитков пользователь получает результат, влияющий на дальнейшее повествование. Поэтому структура игры была разделена на несколько связанных модулей.

При описании архитектуры используются следующие уровни ответственности:
\begin{enumerate}
    \item \textbf{Модуль} -- самостоятельная функциональная часть системы со своей сценарной или координирующей логикой;
    \item \textbf{Подмодуль} -- вспомогательная часть, реализующая отдельную подсистему внутри модуля;
    \item \textbf{Общий компонент} -- переиспользуемый элемент, который подключается из нескольких модулей и не относится только к одному из них.
\end{enumerate}

С учетом данного разделения структура игры включает:

\begin{enumerate}
    \item \textbf{Модуль главного меню -- \texttt{menu.gd}}: запуск игры, открытие настроек и выход из приложения;
    \item \textbf{Подмодуль настроек -- \texttt{settings\_overlay.gd}}: изменение громкости музыкального сопровождения через отдельное окно. Используется в модуле главного меню;
    \item \textbf{Подмодуль паузы -- \texttt{pause\_overlay.gd}}: отображение внутриигрового меню паузы по клавише Esc (продолжить, настройки, переход в меню, выход). Используется в модуле основной новеллы и координирующем модуле ритм-игры;
    \item \textbf{Модуль пролога -- \texttt{prologue.gd}}: начальное введение пользователя в мир игры и подготовка перехода к основной сцене новеллы;
    \item \textbf{Модуль основной новеллы -- \texttt{novel.gd}}: отображение персонажей, реплик и переходов между сюжетными фазами;
    \item \textbf{Модуль концовок -- \texttt{ending.gd}}: выбор и отображение финала на основе результатов прохождения;
    \item \textbf{Координирующий модуль ритм-игры -- \texttt{rhythm\_game.gd}}: управление музыкальной сценой, запуском нот, обработкой ввода и завершением ритм-сцены;
    \item \textbf{Подмодуль обработки файлов нот -- \texttt{sm\_parser.gd}}: чтение \texttt{.sm} файлов и преобразование данных в подходящий формат. Используется координирующим модулем ритм-игры;
    \item \textbf{Подмодуль нот и их поведения -- \texttt{note.gd}, \texttt{hold\_note.gd}}: создание обычных и длинных нот, их движение и обработка удержания;
    \item \textbf{Подмодуль оценки действий игрока -- \texttt{judgment\_lane.gd}, \texttt{score\_service.gd}}: определение точности попаданий, начисление очков и изменение комбо;
    \item \textbf{Подмодуль управления подсветкой ламп -- \texttt{side\_lights\_controller.gd}}: расчет и обновление визуальных параметров бокового освещения в ритм-сцене;
    \item \textbf{Подмодуль библиотеки композиций -- \texttt{songs\_library.gd}}: хранение набора треков и их параметров для ритм-игры;
    \item \textbf{Общий компонент глобального состояния -- \texttt{global\_state.gd}}: хранение общих игровых данных (очки, комбо, текущий клиент, флаги прогресса, параметры продолжения без сохранения в файл), используемых в разных модулях;
    \item \textbf{Общий UI-компонент фоновой анимации -- \texttt{floating\_shapes\_layer.gd}}: визуальный фоновый эффект летающих светлых частиц, переиспользуемый в сценах ритм-игры, меню и новеллы;
    \item \textbf{Общий UI-компонент подсказок управления -- \texttt{controls\_hint.gd}}: оверлей с инструкциями, подключаемый в ритм-сцене.
\end{enumerate}

На рисунке \arabic{img_cnt} представлена диаграмма взаимосвязи модулей друг с другом.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.44\textheight,keepaspectratio]{Модули.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Диаграмма взаимосвязи модулей игры <<Cuppa>>}
    \stepcounter{img_cnt}
\end{figure}

\subsection{Разработка архитектуры и структуры игры}

Общий игровой процесс строится вокруг последовательного прохождения сюжетных и ритм-сцен. На рисунке \arabic{img_cnt} представлена структура игрового процесса. Такая структура позволяет связать ритм-механику с повествованием. Пользователь не просто набирает очки в отдельной мини-игре, а получает результат, который влияет на состояние истории и итоговый финал.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.74\textheight,keepaspectratio]{СтруктураИгровогоПроцесса.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Структура игрового процесса игры <<Cuppa>>}
    \stepcounter{img_cnt}
\end{figure}

\subsection{Диаграмма состояний игры}

Так как игра состоит из нескольких режимов работы, для нее была спроектирована диаграмма состояний. Она показывает, между какими сценами происходит переход и при каких условиях пользователь попадает в следующий игровой блок.

Начальным состоянием является главное меню. Из него игрок может начать игру, открыть настройки или выйти из приложения. При запуске игры открывается пролог, после чего начинается основная сцена новеллы. После завершения вступительной части для текущего посетителя выполняется переход в ритм-игру.

После окончания ритм-сцены система возвращает игрока в новеллу, но уже в состояние результата. В этом состоянии показывается реакция персонажа на приготовленный напиток. Если история еще не завершена, выполняется переход к следующему посетителю. Если все сюжетные сцены пройдены, открывается экран концовки.

На рисунке \arabic{img_cnt} представлена диаграмма состояний игры.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.45\textheight,keepaspectratio]{ДиаграммаСостоянийИгры.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Диаграмма состояний игры <<Cuppa>>}
    \stepcounter{img_cnt}
\end{figure}

\subsection{Проектирование основных алгоритмов}

\subsubsection{Проектирование главного меню и настроек}

Главное меню проектировалось как первая точка взаимодействия пользователя с игрой. В нем предусмотрены три основных действия:

\begin{enumerate}
    \item Начать игру;
    \item Открыть настройки;
    \item Выйти из приложения.
\end{enumerate}

При наведении на пункты меню меняется цвет и масштаб текста, что обеспечивает визуальную обратную связь и показывает, что с элементом можно взаимодействовать. При выборе начала игры открывается сцена пролога, а при выборе выхода приложение завершает работу.

Настройки реализованы в виде отдельного окна, которое открывается поверх текущей сцены меню. В настройках пользователь может изменить громкость музыки с помощью слайдера. При изменении слайдера сначала меняется временное значение, а настоящее значение сохраняется только после нажатия кнопки сохранения. Если пользователь закрывает окно или нажимает Esc, изменения не применяются.

Для текущего прототипа достаточно регулировки громкости, так как для ритм-игры ключевое значение имеет музыкальное сопровождение.

\subsubsection{Проектирование сюжетных сцен новеллы}

Сюжетная часть игры построена как последовательность состояний. Для каждого состояния выбирается свой набор реплик и отображаемый персонаж. В проекте используются два основных типа сюжетных сцен:

\begin{enumerate}
    \item Вступительная сцена посетителя;
    \item Сцена результата после приготовления напитка.
\end{enumerate}

Во вступительной сцене пользователь знакомится с персонажем и получает заказ. После завершения вступительной сцены задается текущий клиент, и игра переходит к ритм-сцене. После ритм-сцены сцена новеллы открывается снова, но уже показывает реакцию персонажа на результат приготовления.

В новелле не используется выбор реплик пользователем. Основная задача этой части -- отображать историю, персонажей и последствия прохождения ритм-сцен. Продвижение по репликам выполняется нажатием клавиши Space или левой кнопки мыши. Также предусмотрена кнопка пропуска, которая позволяет быстрее перейти к следующей фазе.

Для персонажей используются отдельные состояния до и после приготовления напитка. В сцене результата отображается персонаж с кофе. Это помогает визуально показать, что ритм-сцена связана с происходящим в истории, а не является отдельным элементом.

\subsection{Проектирование уровней ритм-игры}

Ритм-уровни проектировались вручную с помощью приложения \texttt{ArrowVortex}, после чего сохранялись в формате \texttt{.sm}. Для каждой композиции формируется массив нотных событий, где для каждой ноты задаются момент появления, номер дорожки и тип ноты.

Для описания одного ритм-уровня используются следующие данные:

\begin{enumerate}
    \item Аудиофайл -- музыкальная композиция, под которую проходит ритм-сцена;
    \item Путь к файлу нот -- расположение \texttt{.sm} файла внутри проекта, например \texttt{res://RhythmGame/Data/Songs/CrimsonPulse.sm};
    \item BPM -- количество ударов в минуту, по которому рассчитывается длительность одного бита;
    \item Offset -- временное смещение карты нот относительно аудио, необходимое для синхронизации музыки и появления нот;
    \item Количество нот -- значение, используемое для вычисления точности прохождения композиции.
\end{enumerate}

Выбор песни зависит от текущего клиента. Такой подход позволяет связать музыкальное сопровождение с конкретной сценой и настроением персонажа.

В ритм-игре используются четыре дорожки, соответствующие клавишам \texttt{S}, \texttt{D}, \texttt{K} и \texttt{L}. Ноты двигаются к линии оценки, а пользователь должен нажимать нужные клавиши в подходящий момент. Ниже введены обозначения и единицы измерения:

\begin{itemize}
    \item $BPM$ -- темп композиции, \textit{уд/мин};
    \item $t_{beat}$ -- длительность одного бита, \textit{с};
    \item $offset$ -- смещение нотной карты относительно начала аудио, \textit{с};
    \item $y_{spawn}$ -- координата точки появления ноты, \textit{пиксели};
    \item $y_{judge}$ -- координата линии оценки, \textit{пиксели};
    \item $N_{travel}$ -- число битов, за которое нота должна дойти до линии оценки;
    \item $t_{move}$ -- время движения ноты от точки появления до линии оценки, \textit{с};
    \item $d$ -- расстояние от точки появления до линии оценки, \textit{пиксели};
    \item $v$ -- скорость движения ноты, \textit{пикс/с};
    \item $b_i$ -- позиция $i$-й ноты в битах от начала карты (например, $b_i=0$ -- начало карты, $b_i=1$ -- через один бит, $b_i=2.5$ -- через два с половиной бита);
    \item $t_i$ -- момент оценки $i$-й ноты на линии, \textit{с};
    \item $t^{spawn}_i$ -- момент создания $i$-й ноты на сцене, \textit{с}.
\end{itemize}

Чтобы ноты появлялись заранее и успевали дойти до линии оценки, выполняются следующие расчеты:

\begin{enumerate}
    \item \textbf{Расчет длительности одного бита.} Длительность одного бита определяется через темп композиции:
    
    \[
    t_{beat} = \frac{60}{BPM}.
    \]
    
    \item \textbf{Расчет времени движения ноты.}
    
    \[
    t_{move} = N_{travel} \cdot t_{beat}, \quad \text{где } N_{travel} \text{ -- константа числа битов.}
    \]

    \item \textbf{Расчет расстояния от точки появления до линии оценки.} Расстояние берется по вертикальной оси по модулю, потому что длина пути всегда должна быть положительной:
    
    \[
    d = \left|y_{spawn} - y_{judge}\right|.
    \]

    \item \textbf{Расчет скорости движения ноты.} Скорость выбирается такой, чтобы нота проходила расстояние $d$ за время $t_{move}$:
    
    \[
    v = \frac{d}{t_{move}}.
    \]

    \item \textbf{Расчет времени оценки и времени появления каждой ноты.}  Для $i$-й ноты сначала определяется момент, когда она должна оказаться на линии оценки:
    
    \[
    t_i = offset + b_i \cdot t_{beat}.
    \]
    Здесь $offset$ задает общее смещение всей карты относительно начала аудио, а $b_i \cdot t_{beat}$ переводит позицию ноты из битов в секунды.

    \item \textbf{Момент создания ноты на сцене.} То есть нота появляется раньше момента оценки ровно на время своего движения:
    
    \[
    t^{spawn}_i = t_i - t_{move}.
    \]
\end{enumerate}

За счет этих параметров музыка и визуальное движение нот воспринимаются как единый процесс.

Перед первым запуском ритм-сцены отображается подсказка управления. Она появляется только один раз, чтобы пользователь понял, какие клавиши используются в игре. После подтверждения подсказки запускается ритм-сцена, а подсказка закрывается.

\subsection{Алгоритм обработки файла нот}

Для генерации нот используется файл формата \texttt{.sm}. Он содержит данные о расположении обычных и длинных нот. Перед началом ритм-сцены файл необходимо прочитать и преобразовать в массив игровых событий, с которым уже может работать проект в ритм-сцене без дополнительной подготовки.

На рисунке \arabic{img_cnt} представлена последовательность действий обработки \texttt{.sm} файла.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.76\textheight,keepaspectratio]{sm_parser.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Последовательность работы подмодуля обработки файлов нот}
    \stepcounter{img_cnt}
\end{figure}

Данный алгоритм включает несколько последовательных проверок и циклов. Сначала выполняются две обязательные проверки: удалось ли открыть файл и найден ли маркер начала нотных данных \texttt{0,0,0,0,0:}. Если хотя бы одно из этих условий не выполнено, подмодуль завершает работу досрочно и возвращает пустой массив событий.

После успешной проверки запускается внешний цикл по тактам композиции. Внутри каждого такта выполняется цикл по строкам, из которых отбираются только строки длиной 4 символа, соответствующие четырем дорожкам. Если после фильтрации строк в такте не осталось, выполняется переход к следующему такту. Далее для валидного такта запускается цикл по строкам и вложенный цикл по дорожкам от 1 до 4, где каждый символ интерпретируется как тип события.

В этой части используются ветвления по символу ноты: при \texttt{1} сразу создается событие обычной ноты, при \texttt{2} запоминается время начала длинной ноты, при \texttt{3} сначала проверяется наличие сохраненного начала удержания для данной дорожки, после чего создается событие длинной ноты с рассчитанной длительностью. После обработки каждой строки увеличивается внутреннее игровое время, что обеспечивает синхронизацию будущих нот с ритмом.

\subsection{Алгоритм ритм-игры}

Основная ритм-сцена отвечает за запуск музыки, генерацию нот, обработку игрового цикла и завершение раунда. Перед началом сцены определяется текущий клиент, по нему выбирается песня, после чего рассчитываются параметры движения нот. К таким параметрам относятся длительность одного бита, время движения ноты к линии оценки и скорость движения.

На рисунке \arabic{img_cnt} представлена схема алгоритма ритм-игры.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.78\textheight,keepaspectratio]{rhythm_game.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Схема алгоритма ритм-игры}
    \stepcounter{img_cnt}
\end{figure}

После подготовки данных сцена проверяет, показывалась ли игроку подсказка управления. Если подсказка еще не была показана, игра переходит в режим ожидания подтверждения, в котором основной ритм-цикл временно не выполняется. После подтверждения подсказки запускается основной цикл ритм-игры. В нем обновляется время, проверяется необходимость создания новых нот, запускается аудио и обновляется интерфейс. Дополнительно во время ритм-сцены поддерживается пауза по клавише Esc с возможностью продолжить игру, открыть настройки, перейти в меню или выйти из приложения.

Ноты создаются не в момент их оценки, а заранее. Это нужно для того, чтобы они успели пройти путь от точки появления до линии оценки. Если в очереди есть нота, время которой уже наступило, система создает обычную или длинную ноту на нужной дорожке. Завершение раунда выполняется только при одновременном выполнении нескольких условий: музыка уже была запущена, воспроизведение завершено, очередь нот пуста, на сцене нет активных нот, а флаг завершения еще не установлен. Такая проверка исключает преждевременное или повторное завершение одной и той же ритм-сцены.

По окончании ритм-сцены рассчитывается точность прохождения. Для этого набранные очки сравниваются с максимально возможным количеством очков за обработанные ноты. Если точность ниже 50\%, результат считается слабым и увеличивает счетчик плохих результатов. Затем игра возвращается в сцену новеллы и показывает сюжетную реакцию персонажа.

\subsection{Проектирование обычных и длинных нот}

В ритм-игре используются два типа нот: обычные и длинные. Обычная нота требует одного своевременного нажатия. После попадания она оценивается, удаляется со сцены, а игрок получает очки в зависимости от точности.

Длинная нота устроена иначе. Для нее важен не только момент начала, но и удержание клавиши. При попадании по началу длинной ноты она переходит в состояние удержания. Пока пользователь удерживает клавишу, тело ноты постепенно уменьшается. Если игрок удерживал ноту хоть какое-то время, он получает оценку за длительность удержания. Если нота не была начата, результат переходит в состояние пропуска.

Такой тип нот делает ритм-сцену разнообразнее. Обычные ноты проверяют своевременное нажатие, а длинные требуют удержания клавиши в течение заданного времени. Это помогает связать действия игрока с ритмом музыкального сопровождения и уменьшить однотипность нажатий.

\subsection{Алгоритм оценки действий игрока}

Для определения точности нажатия используется алгоритм судьи. Его задача -- определить, насколько близко нота находится к линии оценки в момент нажатия клавиши. Игрок использует четыре клавиши: \texttt{S}, \texttt{D}, \texttt{K} и \texttt{L}. Каждая клавиша соответствует своей дорожке, поэтому при нажатии система обрабатывает только ноты на выбранной линии.

На рисунке \arabic{img_cnt} представлена схема алгоритма оценки действий игрока.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.68\textheight,keepaspectratio]{Джаджмент.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Схема алгоритма оценки действий игрока}
    \stepcounter{img_cnt}
\end{figure}

После нажатия клавиши система определяет дорожку и ищет ближайшую ноту в зоне оценки. Если нота не найдена, нажатие не обрабатывается. Если нота найдена, рассчитывается расстояние между нотой и линией оценки, после чего выставляется результат попадания. Дополнительно на дорожке отображается цветовая обратная связь, чтобы пользователь сразу видел итог своего действия. За счет этого ритм-сцена становится понятнее: игрок получает не только числовую оценку, но и визуальный отклик.

В проекте используются следующие оценки попадания:

\begin{enumerate}
    \item \texttt{Bloody bite} -- лучшая оценка, визуально подсвечивается ярко-розовым цветом;
    \item \texttt{Bloody} -- хорошая оценка, визуально подсвечивается ярко-оранжевым цветом;
    \item \texttt{Lame} -- базовая оценка, визуально подсвечивается ярко-бирюзовым цветом;
    \item \texttt{Miss} -- нота пропущена, худшая оценка, визуально подсвечивается темно-фиолетовым цветом.
\end{enumerate}

Критерии оценок и баллов для обычных нот:

\begin{enumerate}
    \item \texttt{Bloody bite} -- расстояние меньше 50 пикселей, начисляется 10 очков;
    \item \texttt{Bloody} -- расстояние меньше 80 пикселей, начисляется 5 очков;
    \item \texttt{Lame} -- расстояние меньше 110 пикселей, начисляется 1 очко;
    \item \texttt{Miss} -- нота пропущена, очки не начисляются, комбо сбрасывается.
\end{enumerate}

При успешном попадании значение комбо увеличивается на 10. Если игрок пропускает ноту, комбо сбрасывается.

Для длинных нот оценка выставляется по прогрессу удержания (отношению удержанной части к полной длине ноты):

\begin{enumerate}
    \item \texttt{Bloody bite} -- прогресс удержания не менее 0.85, начисляется 10 очков;
    \item \texttt{Bloody} -- прогресс удержания не менее 0.50, начисляется 5 очков;
    \item \texttt{Lame} -- прогресс удержания меньше 0.50, начисляется 1 очко;
    \item \texttt{Miss} -- длинная нота не была начата вовремя и вышла из зоны оценки, очки не начисляются.
\end{enumerate}

Такой подход позволяет учитывать не только точность начального нажатия, но и длительность удержания в ритме музыкального сопровождения.

\subsection{Алгоритм выбора концовки}

Концовки проектировались как итог связи ритм-механики и повествования. Игрок не выбирает финал напрямую. Вместо этого итог зависит от качества прохождения ритм-сцен.

После каждой ритм-сцены система рассчитывает точность попаданий в процентах. Сначала определяется максимально возможный балл для уже обработанных нот: количество обработанных нот умножается на 10 (максимум за одну ноту). Затем фактически набранные очки делятся на этот максимум и переводятся в проценты:
\[
Accuracy = \frac{Score}{JudgedCount \cdot 10} \cdot 100\%.
\]
Если обработанных нот нет, точность принимается равной 0\%. Если полученная точность меньше 50\%, текущий результат считается слабым, и счетчик плохих результатов увеличивается на единицу.

На рисунке \arabic{img_cnt} представлена схема алгоритма выбора концовки.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.465\textheight,keepaspectratio]{КонцовкаИгры.drawio.png}
    \caption*{Рисунок \arabic{img_cnt} -- Схема алгоритма выбора концовки}
    \stepcounter{img_cnt}
\end{figure}

После завершения истории система проверяет количество слабых результатов. Если таких результатов меньше двух, выбирается хорошая концовка. Если плохих результатов два или больше, выбирается плохая концовка. Таким образом, финал зависит не от отдельного нажатия, а от общего качества прохождения нескольких ритм-сцен.

Экран концовки отображает набор финальных реплик. Пользователь продвигается по репликам с помощью Space или левой кнопки мыши. После последней реплики появляется кнопка <<Дальше>>. При ее нажатии состояние истории сбрасывается, счетчик плохих результатов обнуляется, и игра возвращается в главное меню.

\subsection{Заключение}

В данном разделе была рассмотрена проектная часть однопользовательской 2D-игры <<Cuppa>>. Были описаны структура игрового процесса, диаграмма состояний, работа главного меню, настроек, сюжетных сцен, ритм-уровней, обработки нот, оценки действий игрока и выбора концовки.

В ходе проектирования было определено, что основой игры является переход между сюжетными сценами и ритм-сценами приготовления кофе. Ритм-механика обеспечивает взаимодействие пользователя с музыкой, а сюжетная часть связывает результаты прохождения с персонажами и финалом истории.

Таким образом, проектирование позволило сформировать целостную структуру игры, в которой меню, настройки, новелла, ритм-игра, система очков и концовки работают как связанные части одного игрового процесса.

\section{Реализация}

\subsection{Выбор инструментария и настройка проекта}

Для реализации игры <<Cuppa>> использовался игровой движок Godot 4.6 и язык GDScript. Выбор Godot обусловлен практическими преимуществами для учебного игрового проекта: движок быстро устанавливается, имеет относительно небольшой размер и встроенные инструменты редактора. За счет этого снижается порог входа в разработку, упрощается перенос рабочей среды между компьютерами и ускоряется процесс разработки.

Проект был организован по тематическим модулям:
\begin{enumerate}
    \item \texttt{Menu} -- главное меню, настройки и пауза;
    \item \texttt{Novel} -- пролог, основная новелла и концовка;
    \item \texttt{RhythmGame} -- ритм-сцена, парсер карт и система оценки действий игрока.
\end{enumerate}

\subsection{Архитектура проекта}

В разделе проектирования модуль, подмодуль и общий компонент определяются как логические единицы ответственности. В разделе реализации эти же единицы рассматриваются с точки зрения физического размещения в проекте: какие каталоги и файлы обеспечивают реализацию соответствующих модулей.

Таким образом, проектирование фиксирует состав и ответственность компонентов, а реализация показывает их структуру в дереве проекта без изменения принятой модульной модели.

\textbf{Структура проекта:}

\begin{itemize}
    \item \texttt{res://Assets/Fonts} -- общие шрифты интерфейса.

    \item \texttt{res://Menu/} -- каталог реализации модуля главного меню и связанных подмодулей.
    \begin{itemize}
        \item \texttt{res://Menu/Scenes/} -- сцены меню, настроек и паузы;
        \item \texttt{res://Menu/Scripts/Menu/} -- логика главного меню;
        \item \texttt{res://Menu/Scripts/Settings/} -- логика окна настроек;
        \item \texttt{res://Menu/Scripts/Pause/} -- логика паузы;
        \item \texttt{res://Menu/Assets/Background/} -- фоновые изображения меню;
        \item \texttt{res://Menu/Data/Songs/} -- музыкальные ресурсы.
    \end{itemize}

    \item \texttt{res://Novel/} -- каталог реализации модулей пролога, основной новеллы и концовок.
    \begin{itemize}
        \item \texttt{res://Novel/Scenes/} -- сцены пролога, основной новеллы и концовки;
        \item \texttt{res://Novel/Scripts/Flow/} -- логика фаз истории;
        \item \texttt{res://Novel/Scripts/UI/} -- логика подсказки в начале ритм-игры;
        \item \texttt{res://Novel/Assets/Backstory/} -- фоновые изображения пролога;
        \item \texttt{res://Novel/Assets/Novel/} -- фоновые изображения новеллы;
        \item \texttt{res://Novel/Assets/Sprites/} -- спрайты персонажей;
        \item \texttt{res://Novel/Data/Dialog/} -- данные диалогов;
        \item \texttt{res://Novel/Data/Songs/} -- музыкальные ресурсы.
    \end{itemize}

    \item \texttt{res://RhythmGame/} -- каталог реализации координирующего модуля ритм-игры и его подмодулей.
    \begin{itemize}
        \item \texttt{res://RhythmGame/Scenes/} -- игровая сцена, сцены нот и линии оценки;
        \item \texttt{res://RhythmGame/Scripts/Controllers/} -- управляющая логика ритм-раунда;
        \item \texttt{res://RhythmGame/Scripts/Gameplay/} -- основная игровая логика;
        \item \texttt{res://RhythmGame/Scripts/Parsing/} -- логика парсера \texttt{.sm} файлов;
        \item \texttt{res://RhythmGame/Scripts/State/} -- хранение игровых данных;
        \item \texttt{res://RhythmGame/Scripts/UI/} -- логика визуальных эффектов ритм-сцены;
        \item \texttt{res://RhythmGame/Assets/Sprites/} -- изображения фона, нот и линии оценки;
        \item \texttt{res://RhythmGame/Assets/VFX/Effects/} -- визуальные эффекты;
        \item \texttt{res://RhythmGame/Data/Songs/} -- музыкальные ресурсы.
    \end{itemize}
    
\end{itemize}

В данной структуре проекта каждый модуль и подмодуль реализует четко определенную функцию, а каталоги проекта отражают эту границу ответственности на уровне кода и ресурсов. Это повышает масштабируемость, упрощает навигацию и снижает сложность сопровождения.

\subsection{Реализация игрового движка}

В рамках реализации игрового движка была собрана целостная игровая система, где визуальные и звуковые ресурсы, интерфейс и модули игровой логики работают как связанные части единого процесса.

\subsubsection{Создание игровых ресурсов}

Для подготовки контента был выделен отдельный этап, так как именно визуальная и звуковая часть определяют восприятие игрового мира, обеспечивают читаемую обратную связь и повышают вовлеченность пользователя.

\textbf{Создание графических ресурсов включало:}
\begin{enumerate}
    \item Ручную отрисовку базовых ассетов в графическом редакторе \texttt{Aseprite};
    \item Подготовку промтов в \texttt{ChatGPT} и генерацию анимаций по отрисованному материалу, а также генерацию отдельных изображений в \texttt{Leonardo.AI} с использованием разных моделей. Сгенерированные изображения переводились в пиксельный формат с помощью отдельного конвертора в режиме \texttt{Mono}, после чего дорабатывались вручную в \texttt{Aseprite}.
\end{enumerate}

\textbf{Создание звуковых ресурсов включало:}
\begin{enumerate}
    \item Подготовку промтов в \texttt{ChatGPT} и генерацию музыкального сопровождения в \texttt{Suno} с последующей ручной обработкой в аудиоредакторе \texttt{Audacity}.
\end{enumerate}

\textbf{Созданные ресурсы включают:}
\begin{enumerate}
    \item Спрайты персонажей и их состояний;
    \item Локации и элементы окружения;
    \item Обычные и длинные ноты;
    \item Линия оценки;
    \item Визуальные эффекты;
    \item Иконка приложения;
    \item Трек для меню и сюжетной части;
    \item Три композиции для ритм-сцены.
\end{enumerate}

Все подготовленные материалы сгруппированы по функциональным каталогам и подключены в проект.

\subsubsection{Реализация главного меню}

Главное меню реализовано в сцене \texttt{Menu/Scenes/Menu.tscn} на базе \texttt{Node2D}, в которой объединены фоновая анимация (\texttt{AnimatedSprite2D}), интерактивные текстовые кнопки (\texttt{Label}) и музыкальное сопровождение (\texttt{AudioStreamPlayer2D}) с автозапуском зацикленного трека \texttt{MenuTheme}. Логика взаимодействия вынесена в скрипт \texttt{Menu/Scripts/Menu/menu.gd}: он обрабатывает наведение и нажатие, запускает визуальный отклик и выполняет переходы между сценами.

В качестве интерактивных кнопок используются \texttt{Label}: команды <<Начать>>, <<Продолжить>>, <<Настройки>> и <<Выход>>. Кнопка <<Начать>> переводит пользователя в пролог, <<Настройки>> открывает оверлей настроек, <<Выход>> завершает работу игры. Команда <<Продолжить>> активируется только при наличии сохранённой текущей сессии, обеспечивая возврат в незавершённое прохождение части игры.

\subsubsection{Реализация настроек}

Окно настроек реализовано в сцене \texttt{Menu/Scenes/Settings.tscn} как оверлей на \texttt{CanvasLayer} с подключением скрипта \texttt{Menu/Scripts/Settings/settings\_overlay.gd}, поскольку такой формат позволяет открывать параметры поверх активной сцены без разрыва игрового процесса. Использование \texttt{ColorRect} с эффектом размытия фона нужно для сохранения контекста текущей сцены и визуальным фокусированием на настройках; \texttt{Panel} задаёт границы рабочей области, а контейнеры \texttt{VBoxContainer}/\texttt{HBoxContainer} обеспечивают аккуратную компоновку элементов.

Три \texttt{Label} отвечают за заголовок <<Настройки>>, подпись параметра <<Музыка>> и отображение значения громкости в процентах; \texttt{HSlider} измененяет громкость, два \texttt{Button} отвечают либо за подтверждение и применение параметра слайдера, либо за выход без подтверждения с помощью кнопки <<Назад>>. Ползунок изменяет черновое значение громкости, а фактическое применение изменений выполняется только по команде <<Сохранить>>, что снижает риск случайных изменений и делает поведение окна предсказуемым.

Внешний вид окна настроек показан на рисунке \arabic{img_cnt}
\begin{figure}[H]
    \centering
    \includegraphics[height=0.3\textheight,keepaspectratio]{setting.png}
    \caption*{Рисунок \arabic{img_cnt} -- Окно настроек}
    \stepcounter{img_cnt}
\end{figure}

\subsubsection{Реализация паузы}

Подмодуль паузы реализован в сцене \texttt{Menu/Scenes/PauseOverlay.tscn} как отдельный оверлей на \texttt{CanvasLayer} со скриптом \texttt{Menu/Scripts/Pause/pause\_overlay.gd}. Использование \texttt{ColorRect} с размытием и затемняющим слоем нужно для того, чтобы остановить игровой контекст визуально, но сохранить его читаемость; \texttt{Panel} и контейнер \texttt{VBoxContainer} формируют компактное размещение элементов формы в центре экрана. При открытии оверлея сцена сзади переводится в режим паузы, что предотвращает фоновое выполнение игровой логики.

В качестве интерактивных элементов используются несколько \texttt{Button}: <<Продолжить>>, <<Настройки>>, <<В меню>> и <<Выйти>>. Кнопка <<Продолжить>> возвращает пользователя в активный игровой процесс, <<Настройки>> открывает \texttt{SettingsOverlay} поверх паузы, команда <<В меню>> сохраняет путь текущей сцены для последующего продолжения, а <<Выйти>> завершает работу игры. Тем самым пауза выступает не только как остановка игры, но и как общий интерфейс управления вспомогательными действиями.

Внешний вид окна внутриигрового меню показан на рисунке \arabic{img_cnt}.
\begin{figure}[H]
    \centering
    \includegraphics[height=0.4\textheight,keepaspectratio]{pause.png}
    \caption*{Рисунок \arabic{img_cnt} -- Окно внутриигрового меню}
    \stepcounter{img_cnt}
\end{figure}

\subsubsection{Реализация пролога}

Пролог реализован в сцене \texttt{Novel/Scenes/Prologue.tscn} на базе \texttt{Node2D} с подключением скрипта \texttt{Novel/Scripts/Flow/prologue.gd}. 

В сцене используются \texttt{Sprite2D}, выступающих в роли фона, а также два \texttt{Label} -- \texttt{Role} и \texttt{Dialoge}, отвечающие за отображение роли говорящего и текущей реплики. За музыкальное сопровождение отвечает \texttt{AudioStreamPlayer2D} с автозапуском зацикленного трека \texttt{Prologue}. Такая структура поддерживает атмосферу вступления и обеспечивает отображение текста.

Интерактивная часть построена на трёх каналах управления: клавиша Space, левая кнопка мыши и кнопка <<Пропустить>>. В \texttt{prologue.gd} массив реплик загружается из \texttt{prologue\_lines.gd}, после чего функции \texttt{show\_line()} и \texttt{next\_line()} последовательно обновляют содержимое \texttt{Role} и \texttt{Dialoge}. При достижении конца массива или при нажатии <<Пропустить>> запускается единый переход к следующей сцене.

Для смены сцены используется плавное затемнение, что убирает резкий визуальный разрыв между эпизодами. Дополнительно при запуске пролога обновляется глобальное состояние продолжения, поэтому пользователь может корректно вернуться к текущему этапу из главного меню. 

\subsubsection{Реализация новеллы}

Основная новелла реализована в сцене \texttt{Novel/Scenes/MainNovel.tscn} на базе \texttt{Node2D} с подключением скрипта \texttt{Novel/Scripts/Flow/novel.gd}. 

В сцене используются \texttt{AnimatedSprite2D} для фона и персонажей, \texttt{Panel} для области диалога, \texttt{Label} -- \texttt{Role} и \texttt{DialogText} для вывода роли и текста реплики, \texttt{Button} -- <<Пропустить>> для пропуска сцны новеллы, \texttt{AudioStreamPlayer2D} с автозапуском зацикленного трека \texttt{Novel}. 

В модуле используются все спрайты персонажей и их состояний. Примеры спрайтов показаны на рисунках ниже:

\begin{figure}[H]
    \centering
    \includegraphics[height=0.175\textheight,keepaspectratio]{MaryCalm.png}
    \caption*{Рисунок \arabic{img_cnt} -- Спрайт Мэри}
    \stepcounter{img_cnt}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.25\textheight,keepaspectratio]{MaryWithCoffe.png}
    \caption*{Рисунок \arabic{img_cnt} -- Спрайт Мэри с кофе}
    \stepcounter{img_cnt}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.175\textheight,keepaspectratio]{KoriAnim.png}
    \caption*{Рисунок \arabic{img_cnt} -- Спрайт Кори}
    \stepcounter{img_cnt}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.175\textheight,keepaspectratio]{KoriWithCoffe.png}
    \caption*{Рисунок \arabic{img_cnt} -- Спрайт Кори с кофе}
    \stepcounter{img_cnt}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.175\textheight,keepaspectratio]{RoseAnim.png}
    \caption*{Рисунок \arabic{img_cnt} -- Спрайт Роуз}
    \stepcounter{img_cnt}
\end{figure}
.
\begin{figure}[H]
    \centering
    \includegraphics[height=0.175\textheight,keepaspectratio]{RoseWithCoffe.png}
    \caption*{Рисунок \arabic{img_cnt} -- Спрайт Роуз с кофе}
    \stepcounter{img_cnt}
\end{figure}

Сценарная логика опирается на данные из \texttt{Novel/Data/Dialog/dialogs.gd}, где диалоги организованы по фазам:
\begin{enumerate}
    \item \texttt{mary\_intro};
    \item \texttt{mary\_happy};
    \item \texttt{mary\_sad};
    \item \texttt{cory\_intro};
    \item \texttt{cory\_happy};
    \item \texttt{cory\_sad};
    \item \texttt{rose\_intro};
    \item \texttt{rose\_happy};
    \item \texttt{rose\_sad};
    \item \texttt{rose\_bad\_ending}.
\end{enumerate}

В \texttt{novel.gd} выбирается нужный набор реплик по текущему состоянию новеллы, скрывает неактуальные спрайты и показывает только персонажа текущей фазы.

Интерактивная часть построена на клавише Space, левой кнопке мыши, кнопке <<Пропустить>> и клавише Esc. Команды Space/ЛКМ переводят диалог на следующую реплику, <<Пропустить>> пропускает текущую сюжетную сцену и переходит к ритм-игре, а Esc открывает оверлей паузы.

Для смены сцены используется плавное затемнение, что убирает резкий визуальный разрыв между эпизодами. Дополнительно при запуске новеллы обновляется глобальное состояние продолжения, поэтому пользователь может корректно вернуться к текущему этапу из главного меню.

\subsubsection{Реализация ритм-игры}

Ритм-игра реализована как отдельный модуль, в котором сцены, скрипты парсинга, логика генерации нот, система оценки и визуальные эффекты работают как единый игровой цикл. По структуре проектирования эта часть соответствует координирующему модулю \texttt{rhythm\_game.gd} и набору специализированных подмодулей.

Сцена раунда реализована в файле \texttt{RhythmGame/Scenes/RhythmGame.tscn} и построена на \texttt{Node2D}. 

\textbf{Сцена включает в себя следующие ключевые элементы:}

\begin{enumerate}
    \item Точки спавна нот: \texttt{SpawnPoint1, SpawnPoint2, SpawnPoint3, SpawnPoint4};
    \item Зона оценки: \texttt{JudgmentRoot};
    \item Аудиоплеер трека: \texttt{AudioStreamPlayer2D};
    \item Индикаторы состояния: \texttt{Score}, \texttt{Combo}, \texttt{ComboScore}, \texttt{ProgressBar};
    \item Фон: \texttt{AnimatedSprite2D};
    \item Визуальные эффекты: \texttt{FloatingShapesLayer}, \texttt{LightLeft/LightRight};
    \item Оверлей подсказки управления перед первым запуском ритм-сцены.
\end{enumerate}

\textbf{Для ритм-объектов используются три отдельные сцены:}
\begin{enumerate}
    \item \texttt{RhythmGame/Scenes/Note.tscn} -- обычная нота на базе \texttt{Area2D} с хитбоксом, спрайтом ноты и визуальным эффектом подсвечивания. Внешний вид обычной и длинной ноты показан на рисунке \ref{fig:rh_note_hold_compare}. Эффект ноты изображен на рисунке \ref{fig:rh_note_effect};
    \item \texttt{RhythmGame/Scenes/LongNote.tscn} -- длинная нота, состоящая из головы, тела и хвоста, с отдельным хитбоксом и расчётом длины через длительность удержания. Внешний вид частей длинной ноты показан на рисунке \ref{fig:rh_hold_parts};
    \item \texttt{RhythmGame/Scenes/Judgment.tscn} -- зона оценки и визуальные анимации отклика при попадании. Внешний вид ассета показан на рисунке \ref{fig:rh_judgment_asset}. Эффект судьи изображен на рисунке \ref{fig:rh_judgment_effect}.
\end{enumerate}

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.47\textwidth}
        \centering
        \includegraphics[height=0.16\textheight,keepaspectratio]{Note.png}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.47\textwidth}
        \centering
        \includegraphics[height=0.16\textheight,keepaspectratio]{LongNote.png}
    \end{minipage}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Ноты: обычная нота и длинная нота}
    \label{fig:rh_note_hold_compare}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.31\textwidth}
        \centering
        \includegraphics[height=0.11\textheight,keepaspectratio]{HeadNote.png}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.31\textwidth}
        \centering
        \includegraphics[height=0.11\textheight,keepaspectratio]{BodyNote.png}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.31\textwidth}
        \centering
        \includegraphics[height=0.11\textheight,keepaspectratio]{BottomNote.png}
    \end{minipage}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Части длинной ноты: верхняя часть, тело, нижняя часть}
    \label{fig:rh_hold_parts}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.033\textheight,keepaspectratio]{Judgment.png}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Линия оценки}
    \label{fig:rh_judgment_asset}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.16\textheight,keepaspectratio]{NoteEffect.png}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Эффект ноты}
    \label{fig:rh_note_effect}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.16\textheight,keepaspectratio]{JudgmentEFfect.png}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Эффект нажатия клавиши у дорожки зоны оценки}
    \label{fig:rh_judgment_effect}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[height=0.20\textheight,keepaspectratio]{light.png}
        \refstepcounter{img_cnt}
        \caption*{Рисунок \arabic{img_cnt} -- Боковой свет}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[height=0.20\textheight,keepaspectratio]{Sparkle.png}
        \refstepcounter{img_cnt}
        \caption*{Рисунок \arabic{img_cnt} -- Светящиеся частицы}
    \end{minipage}
\end{figure}

Перед первым запуском ритм-сцены пользователю показывается отдельная подсказка управления с набором рабочих клавиш. Подсказка отображается только один раз, после подтверждения закрывается и не прерывает последующие раунды.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.22\textheight,keepaspectratio]{hint.png}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Подсказка управления перед первой ритм-сценой}
    \label{fig:rh_controls_hint}
\end{figure}

\textbf{UI-подмодули визуальной обратной связи.}
\begin{enumerate}
    \item \texttt{RhythmGame/Scripts/UI/side\_lights\_controller.gd} -- синхронно обновляет прозрачность левых и правых источников света через синусоидальный пульс;
    \item \texttt{RhythmGame/Scripts/UI/pop\_effect.gd} -- запускает короткую анимацию <<pop>> для текста комбо и усиливает ощущение отклика;
    \item \texttt{RhythmGame/Scripts/UI/floating\_shapes\_layer.gd} -- формирует слой плавающих частиц с рандомным масштабом, скоростью и пульсацией прозрачности, создавая живой фон ритм-сцены.
\end{enumerate}

\subsubsection{Реализация концовок новеллы}

Модуль концовки реализован в сцене \texttt{Novel/Scenes/Ending.tscn} на базе \texttt{Node2D} с подключением скрипта \texttt{Novel/Scripts/Flow/ending.gd}. Сцена построена так, чтобы в рамках одного экрана поддерживать два варианта финала и единый диалоговый интерфейс.

В состав сцены входят следующие ключевые элементы:
\begin{enumerate}
    \item \texttt{GoodEnding} -- слой хорошей концовки с фоновым изображением;
    \item \texttt{BadEnding} -- полностью затемняющий \texttt{ColorRect};
    \item \texttt{Role} и \texttt{DialogText} -- \texttt{Label} для отображения говорящего и текста реплики;
    \item \texttt{Button} (<<Дальше>>) -- кнопка выхода из концовки после показа всех реплик.
\end{enumerate}

\textbf{Логика выбора финала}: если плохих результатов меньше, чем 2, то активируется хорошая концовка, иначе -- плохая. Для каждой ветки загружается свой набор реплик из \texttt{Novel/Data/Dialog/ending\_lines.gd}, после чего экран последовательно показывает реплики.

\textbf{Хорошая концовка.}
Хорошая ветка показывает развитие истории после успешного прохождения ритм-сцен: кофейня становится местом сосуществования людей и вампиров, а героиня принимает предложение алхимика о совместной работе. 

\textbf{Плохая концовка.}
Сценарный блок сокращен до коротких реплик рассказчика: в ней главная героиня умирает от рук охотницы Роуз. Деятельность кофейни больше не имеет продолжения. 

\subsection{Реализация управления в ритм-игре}

В данном подпункте рассматривается практическая реализация управления, которые были определены на этапе проектирования. Основу взаимодействия в ритм-сцене составляет четырехклавишная схема управления: \texttt{S}, \texttt{D}, \texttt{K}, \texttt{L}. Каждая клавиша закреплена за своей дорожкой оценки.

Обработка ввода реализуется в связке координирующего скрипта \texttt{rhythm\_game.gd} и подмодуля дорожки оценки \texttt{judgment\_lane.gd}. Для обычной ноты итог фиксируется сразу. Для длинной ноты используется двухэтапная механика. Первое нажатие фиксирует начало удержания, после чего логика переходит в \texttt{hold\_note.gd}. Во время удержания тело ноты постепенно уменьшается, что визуально показывает прогресс выполнения. При отпускании клавиши рассчитывается доля удержания и назначается итоговая оценка.

\subsection{Эволюция интерфейса и визуального решения}

В ходе разработки визуальная часть проекта проходила через несколько итераций: от ранних прототипов и черновых макетов до текущего интерфейса ритм-сцены.

Ниже приведены скриншоты эволюции интерфейса и их подписи:
рисунок \ref{fig:evo_mockup_1}, рисунок \ref{fig:evo_mockup_2}, рисунок \ref{fig:evo_prev_1}, рисунок \ref{fig:evo_prev_2}, рисунок \ref{fig:evo_prev_3}, рисунок \ref{fig:evo_final}.

\begin{figure}[H]
    \centering
    \includegraphics[height=0.24\textheight,keepaspectratio]{1.jpg}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Нефинальный макет ритм-интерфейса (вариант 1)}
    \label{fig:evo_mockup_1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.24\textheight,keepaspectratio]{2.jpg}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Нефинальный макет ритм-интерфейса (вариант 2)}
    \label{fig:evo_mockup_2}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.24\textheight,keepaspectratio]{3.jpg}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Предыдущая версия интерфейса ритм-сцены}
    \label{fig:evo_prev_1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.24\textheight,keepaspectratio]{6.jpg}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Предшествующая версия визуального решения}
    \label{fig:evo_prev_2}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.24\textheight,keepaspectratio]{7.jpg}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Ранний прототип интерфейса и игровых элементов}
    \label{fig:evo_prev_3}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.24\textheight,keepaspectratio]{8.jpg}
    \refstepcounter{img_cnt}
    \caption*{Рисунок \arabic{img_cnt} -- Итоговый макет, на основе которого сформирована текущая версия интерфейса}
    \label{fig:evo_final}
\end{figure}

\subsection{Экспериментальная апробация}

Экспериментальная апробация выполнялась не только как линейное прохождение, но и как серия повторных проверок в разных состояниях интерфейса и игрового цикла.

\textbf{Проверка базовых пользовательских сценариев:}
\begin{enumerate}
    \item Запуск игры через главное меню, переход в пролог и переход в основную новеллу;
    \item Прохождение сюжетной сцены с переходом к ритм-раунду;
    \item Обработка обычных и длинных нот, включая старт удержания и досрочное отпускание;
    \item Возврат в новеллу после завершения ритм-сцены;
    \item Проверка выдачи двух концовок игрой.
\end{enumerate}

\textbf{Проверка интерфейса и устойчивости к повторным действиям:}
\begin{enumerate}
    \item Многократные нажатия на кнопки меню, настроек и паузы в разных состояниях сцены;
    \item Повторные открытия и закрытия окна настроек из главного меню и из оверлея паузы;
    \item Проверка реакции кнопок;
    \item Серия быстрых кликов по интерактивным элементам для проверки отсутствия некорректных двойных переходов;
    \item Проверка одноразового показа подсказки управления перед первой ритм-сценой.
\end{enumerate}

\textbf{Проверка отказоустойчивости ритм-подсистемы:}
\begin{enumerate}
    \item Загрузка некорректных \texttt{.sm}-файлов и проверка корректной обработки без аварийного завершения;
    \item Проверка поведения при отсутствии ожидаемой структуры нотных данных;
\end{enumerate}

\textbf{Проверка производительности и кроссплатформенного поведения:}
\begin{enumerate}
    \item Проверка стабильности на персональном компьютере без заметных просадок частоты кадров в основных сценах;
    \item Проверка запуска и прохождения ключевых сценариев на виртуальной машине с низкими техническими характеристиками без критических задержек ввода и визуальных артефактов;
    \item Сравнительная проверка на Linux (среда разработки) и Windows: переходы между сценами, логика управления и обработка ритм-событий отрабатывают одинаково.
\end{enumerate}

По итогам апробации подтверждена работоспособность ключевого игрового цикла, корректность переходов между сценами, устойчивость интерфейса к повторным действиям пользователя, стабильная обработка некорректных входных данных нотных карт и предсказуемое поведение проекта на Linux и Windows, включая запуск в виртуализированной среде.

\section{Заключение}

В рамках курсового проекта была разработана однопользовательская 2D-игра <<Cuppa>>, объединяющая визуальную новеллу и ритм-механику в едином игровом цикле. Основная цель работы заключалась в создании функционального прототипа, где сюжет, музыкальный геймплей и система оценки действий пользователя связаны между собой и влияют на итог прохождения.

В ходе выполнения работы была проанализирована предметная область, сформулированы функциональные и нефункциональные требования, а также спроектирована модульная структура приложения. Для проекта были выделены модули и подмодули с разделением ответственности: главное меню, настройки, пауза, пролог, основная новелла, ритм-игра, модуль концовок и общие UI-компоненты. Такое разделение позволило сохранить целостность архитектуры и упростило сопровождение кода.

На этапе реализации подготовлены графические и аудиоресурсы, настроены сцены и скрипты управления, реализованы алгоритмы ритм-раунда, обработки обычных и длинных нот, система оценки точности и механизм выбора концовки. Отдельное внимание было уделено пользовательскому интерфейсу: добавлены подсказки управления, пауза, настройки и визуальная обратная связь, обеспечивающие понятное взаимодействие пользователя с системой.

Экспериментальная апробация подтвердила корректность ключевых сценариев.

Итогом проекта является работоспособный прототип, демонстрирующий практическую реализуемость выбранного подхода к интеграции визуальной новеллы и ритм-игры. Поставленные в работе задачи выполнены, а полученная структура проекта может использоваться как база для дальнейшего развития: расширения сюжетных веток, улучшение визуала, добавления новых композиций и усложнения ритм-механик без переработки базовой архитектуры.

\end{document}
